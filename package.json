{
  "name": "boxful-rag-bot",
  "version": "0.1.0",
  "description": "RAG chatbot for Boxful Freshdesk knowledge base with selectable embedding providers",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "tsx src/server.ts",
    "build": "tsc -p tsconfig.json",
    "check": "tsc --noEmit -p tsconfig.json",
    "start": "node dist/server.js",
    "ingest": "tsx src/ingest/run.ts"
  },
  "dependencies": {
    "@fastify/cors": "^9.0.1",
    "@fastify/rate-limit": "^9.1.0",
    "@fastify/helmet": "^11.1.0",
    "dotenv": "^16.4.5",
    "fastify": "^4.28.1",
    "node-html-parser": "^6.1.13",
    "p-limit": "^6.1.0",
    "zod": "^3.23.8"
  },
  "devDependencies": {
    "@types/node": "^20.11.30",
    "tsx": "^4.19.2",
    "typescript": "^5.6.3"
  }
}
